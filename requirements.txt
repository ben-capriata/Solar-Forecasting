# Pinned runtime dependencies for the solar forecasting pipeline.
# Versions are the ones the reported results were produced with.
# Python 3.11 or newer is required. CPU-only torch is sufficient, the model is small.
matplotlib==3.11.1
numpy==2.5.1
pandas==2.3.3
pyarrow==23.0.1
PyYAML==6.0.3
requests==2.34.2
scikit-learn==1.9.0
torch==2.13.0
